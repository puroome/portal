# 광남고 포털 이관 절차 (학교를 옮기거나 담당자가 바뀔 때)

지금 이 앱은 **개인 Google 계정(`puroome@gmail.com`) 하나에 묶여** 돌아갑니다.
이 문서는 그 계정에서 손을 뗄 때 무엇을 어떤 순서로 넘겨야 자료가 안 깨지는지 정리한 것입니다.

**가장 좋은 시기는 2월(학년도 말)** 입니다. 방과후·야자·제출이 모두 끝나 서비스가 잠시 멈춰도 되는 때니까요.

---

## ⚠️ 먼저 읽어야 할 한 가지 — 파일을 "복사"하면 안 됩니다

제출 파일은 드라이브에 있지만, **어느 학생의 어느 자료인지는 Firebase 가 기억합니다.**

```
portal/submissions/{프로그램}/{학번}/{자료}
  └ files: [ { id: "1a2B3c...", name: "보고서.pdf", size: 204800 } ]
                    ↑ 드라이브 파일 ID
```

- 파일을 **복사하면 ID 가 새로 생깁니다.** 앱에 남아 있는 옛 ID 는 아무것도 가리키지 못해
  **모든 첨부파일이 "파일을 찾을 수 없음" 이 됩니다.**
- 게다가 권한 확인용 정보(`{sid, programId}`)를 **파일 설명란**에 넣어 두는데, 복사본에는 따라오지 않습니다.
  ID 를 살려 놓더라도 이게 없으면 교사·학생 모두 열지 못합니다.

> **그러므로 파일은 "소유권 이전(공유 → 소유자로 변경)" 으로만 넘깁니다.**
> 다운로드해서 다시 올리는 방식은 절대 쓰지 마세요.

---

## 지금 내 계정에 묶여 있는 것

| # | 자산 | 어디에 | 넘기는 방법 |
|---|---|---|---|
| 1 | **제출 파일** | 내 드라이브 `스쿨포털_제출자료` | 폴더 **소유권 이전** |
| 2 | **관리 스프레드시트 + Apps Script** | 내 드라이브 | 소유권 이전 (스크립트 속성도 함께 따라감) |
| 3 | **웹 앱 배포** | Apps Script 배포 | 새 소유자가 **재배포** → `SCRIPT_URL` 교체 |
| 4 | **Firebase 프로젝트** `record-99cf0` | GCP IAM 소유자 | 새 소유자 추가 → 내 권한 제거 |
| 5 | **앱 호스팅** | GitHub Pages (`puroome.github.io/portal`) | 저장소 이전 또는 새 계정에서 배포 |
| 6 | **스크립트 속성** | `ROOT_FOLDER_ID` · `CURRENT_SCHOOL_YEAR` · `NEW_YEAR_DONE` | 2번을 넘기면 함께 따라감 |
| 7 | **voca · novel** | `wordapp-91c0a` · `novel-91d5f` | 별도 앱. 같이 넘길지 따로 정함 |
| 8 | **record 앱 성적** | `students/` (같은 Firebase) | 4번에 포함 |

---

## 지금 미리 해 두면 좋은 일 (이관 전이라도)

갑작스러운 일에 대비해 **오늘 해 둘 수 있는 두 가지**입니다. 서비스에는 아무 영향이 없습니다.

1. **Firebase 에 학교 계정을 공동 소유자로 미리 추가**
   [GCP IAM](https://console.cloud.google.com/iam-admin/iam?project=record-99cf0) → 액세스 권한 부여 → 학교 계정 → 역할 **소유자**
   → 내 계정에 문제가 생겨도 학교가 DB(성적·활동·출결)를 잃지 않습니다.
2. **`스쿨포털_제출자료` 폴더를 학교 계정에 "편집자" 로 공유**
   → 소유권은 그대로지만, 학교가 언제든 자료를 열람·백업할 수 있습니다.

---

## 시나리오 A — 앱과 자료를 모두 후임자(또는 학교 계정)에게 넘긴다

가장 흔한 경우입니다. **위에서 아래 순서 그대로** 하세요. 순서를 바꾸면 중간에 서비스가 끊깁니다.

### 1단계 · 백업 (되돌릴 수 없는 작업 전에)
1. Firebase 콘솔 → Realtime Database → ⋮ → **JSON 내보내기** → 파일 보관
2. 관리 스프레드시트 → 파일 → 사본 만들기 (계정·졸업·전출 시트 전부)
3. `gas/Code.gs` · `js/config.js` 현재 내용을 따로 저장

### 2단계 · 새 관리자 계정 정하기
- **학교 Workspace 계정 권장** (담당자가 바뀌어도 학교에 남습니다)
- 개인 Gmail 로 넘기면 나중에 같은 문제가 반복됩니다

### 3단계 · Firebase 권한 넘기기 (내 권한은 아직 지우지 않습니다)
1. [GCP IAM](https://console.cloud.google.com/iam-admin/iam?project=record-99cf0) → **액세스 권한 부여**
2. 새 관리자 계정 → 역할 **소유자(Owner)** → 저장
3. 새 관리자가 메일의 초대를 수락

### 4단계 · 드라이브 폴더 소유권 이전
1. 내 드라이브 → `스쿨포털_제출자료` 폴더 우클릭 → **공유**
2. 새 관리자 계정 추가 → 권한 **편집자** → 전송
3. 다시 공유 창 → 그 사람 옆 드롭다운 → **소유자로 지정** → 확인
4. 새 관리자가 **소유권 이전 요청을 수락**

> **여기서 막히면** — 개인 Gmail 에서 학교 Workspace 도메인으로는 소유권 이전이 차단되어 있을 수 있습니다.
> 그때는 아래 **"소유권 이전이 안 될 때"** 를 보세요.

### 5단계 · 스프레드시트 소유권 이전
1. 관리 스프레드시트 → 공유 → 새 관리자 **편집자** 추가 → **소유자로 지정**
2. 이전이 끝나면 Apps Script 프로젝트와 **스크립트 속성(`ROOT_FOLDER_ID` 등)도 함께 따라갑니다**
3. 나는 **편집자로 남습니다** (인수인계 기간 동안 도와주려면 필요)

### 6단계 · 새 소유자가 웹 앱 재배포
1. 새 관리자 계정으로 스프레드시트 열기 → 확장 프로그램 → **Apps Script**
2. `Code.gs` 의 `ADMIN_EMAIL` 을 **새 관리자 이메일로 수정** → 저장
3. **배포 → 새 배포 → ⚙️ 웹 앱**, 실행: **나**, 액세스: **모든 사용자** → 배포 → 권한 승인
4. 나온 **새 웹 앱 URL 복사**

### 7단계 · 앱 설정 교체
1. `js/config.js` 의 `SCRIPT_URL` 을 6단계 URL 로 교체
2. GitHub Pages 를 새 계정에서 운영한다면 저장소를 이전하거나 새로 올리고,
   Firebase 콘솔 → Authentication → 설정 → **승인된 도메인**에 새 주소 추가
3. 학생·교사에게 새 주소를 안내 (주소가 바뀌었다면)

### 8단계 · 검증 — 이 6가지가 다 되어야 끝입니다
| 확인할 것 | 방법 | 기대 결과 |
|---|---|---|
| 배포 계정 | 새 `SCRIPT_URL` 을 주소창에 붙여넣기 | `deployedAs` 가 **새 관리자** |
| **옛 첨부파일** | 학생 계정으로 로그인 → 지난 제출자료 열기 | **정상 열림** (여기가 깨지면 파일 ID 문제) |
| 새 업로드 | 학생 계정으로 파일 올리기 | 새 관리자 드라이브에 생성 |
| 교사 기능 | 교사 계정 → 통계·학생별 모아보기 | 정상 |
| ENGLISH | 포털 → voca·novel 버튼 | 자동 로그인 |
| 시트 메뉴 | 새 관리자로 `🔥Firebase 동기화` | 정상 실행 |

### 9단계 · 내 권한 정리 (검증이 끝난 뒤에만)
1. GCP IAM 에서 내 계정 **소유자 권한 제거**
2. 드라이브·스프레드시트 공유 목록에서 내 계정 제거
3. 내 드라이브에 `스쿨포털_제출자료` 가 남아 있지 않은지 확인 (이전됐다면 사라져 있어야 정상)
4. 이전 배포(내 계정 것)는 **보관함으로 이동**하지 말고 그대로 두세요 — 혹시 되돌릴 일이 생깁니다.
   완전히 안정된 뒤(한 학기쯤) 지우면 됩니다.

---

## 시나리오 B — 앱은 내가 가져가고, 이 학교 자료는 남긴다

새 학교에서 새로 시작하고 광남고 자료는 학교에 남기는 경우입니다.

1. **Firebase 프로젝트를 나누는 것이 핵심입니다.** 새 학교용 프로젝트를 새로 만드세요.
   같은 프로젝트를 두 학교가 쓰면 학번이 충돌하고 명단이 섞입니다.
2. 광남고 쪽: 시나리오 A 의 3~9단계를 그대로 (프로젝트·드라이브·시트를 학교에 넘김)
3. 새 학교 쪽: 이 폴더를 그대로 복사해 `js/config.js` 의 Firebase 설정·`SCRIPT_URL` 을 새 프로젝트 값으로 바꾸고,
   `README.md` 의 **설치 순서 1~5** 를 처음부터 다시 수행
4. 코드(이 폴더)는 내 것이므로 그냥 가져가면 됩니다

---

## 시나리오 C — 앱을 종료하고 자료만 보관한다

1. Firebase → Realtime Database → **JSON 내보내기** (활동 내용·출결·성적 전부)
2. 드라이브 `스쿨포털_제출자료` 폴더를 통째로 학교 계정에 **소유권 이전**
   (앱을 안 쓸 것이므로 이때는 복사·다운로드해도 됩니다 — 파일 ID 가 필요 없으니까요)
3. 학생들에게 종료 시점을 미리 안내하고, 필요한 자료는 그 전에 내려받게 합니다
4. `js/config.js` 의 `SCRIPT_URL` 을 비우거나 페이지를 내려 접속을 막습니다

---

## 소유권 이전이 안 될 때

개인 Gmail → 학교 Workspace 도메인은 **조직 정책으로 소유권 이전이 막혀 있는 경우가 많습니다.**
(반대로 학교 → 학교 같은 도메인끼리는 문제없습니다.)

| 대안 | 내용 | 주의 |
|---|---|---|
| **① 계정을 살려 둔다** | 소유권은 그대로 두고 학교 계정을 편집자로 추가. 배포도 계속 내 계정 | Google 계정은 **2년 이상 미사용 시 삭제**될 수 있습니다. 가끔 로그인하세요 |
| **② 중간 계정을 거친다** | 학교의 **개인 Gmail 성격 공용 계정**(예: 부서 Gmail)으로 먼저 이전 | 개인 Gmail 끼리는 이전이 됩니다 |
| **③ 첨부는 새로 시작** | DB(활동 내용·출결·성적)만 넘기고, 파일은 Takeout 으로 내려받아 학교가 따로 보관 | **지난 첨부파일 링크는 앱에서 깨집니다.** 학생 제출 글은 남습니다 |

③ 을 택한다면, 학교가 파일을 잃지 않도록 **학년도별 폴더를 통째로 Takeout 백업**한 뒤 넘기세요.

---

## 절대 하지 말 것

- ❌ 제출 파일을 **복사·다운로드 후 재업로드** → 모든 첨부파일 링크가 깨집니다
- ❌ `스쿨포털_제출자료` 폴더 **이름 변경·삭제** → 스크립트 속성 `ROOT_FOLDER_ID` 와 어긋납니다
- ❌ 검증 전에 **내 Firebase 소유자 권한 제거** → 문제가 생겨도 되돌릴 수 없습니다
- ❌ **학기 중 이관** → 출결 체크인·제출이 진행되는 동안 주소가 바뀌면 혼란이 큽니다
- ❌ `Code.gs` 의 `EMAIL_DOMAIN` · `PW_PREFIX` 변경 → 전교생이 로그인하지 못합니다
