# 광남고 포털 — 작업 안내 (다른 컴퓨터·세션에서 이어받기)

학생·교사가 ID/비밀번호로 로그인해 활동자료·출결·성적·ENGLISH(voca·novel)를 쓰는 통합 앱이다.
**기능·설치·운영 방법은 `README.md` 에 있다. 먼저 그것을 읽는다.** 이 문서는 README 에 없는
결정의 이유, 진행 상황, 남은 일, 작업 방식을 남긴다. (마지막 갱신: 2026-09-19)

## ▶ 다음 세션은 여기부터 (2026-09-19 두 번째 세션 끝)

이 세션에 한 일: 전화번호 열·📞전화/📩문자, record 정리(Coge.gs: 비밀번호·admin·old 제거), 🎓졸업생 앨범(record old 이관 완료),
동기화 = 시트가 정답(행 삭제 → 확인 후 잠금, 되살리기). 각 절 참고.

사용자가 한 것(확인됨): record 졸업생 가져오기 실행 → 포털에서 확인, record `admin`·`old` 시트 삭제 + record 데이터 동기화 성공.

원래 요청 3번(공지·급식·행사)까지 **모두 구현**. 🍴급식·📅행사는 사용자가 실제로 써 보고 "잘된다" 확인.
🪧공지는 2026-09-19 구현(아래 절) — **첫 질문으로 확인할 것**: 최종 규칙(`database.rules.json`, notices 추가됨) 다시 게시·Code.gs 붙여넣기·Ctrl+F5 했는지.
급식 사진은 NEIS 에 없음(필드 15개 확인). 학교 홈페이지(gwangnam.jge.hs.kr) 긁어오기는 사용자가 "됐어, 충분해" 로 접음.
야간자율은 사용자 결정으로 **지금처럼 모든 교사가 편집**(예외). 빛나다·심화탐구·방과후는 이미 "모두 열람·만든 교사만 수정"(확인함).

사용자가 Code.gs 최신본·record C열 삭제·전화번호 열까지 끝냈다고 함. 그다음 **최종 보안 규칙 교체**를 준비해 안내함 —
**첫 질문으로 확인할 것**: 최종 규칙을 게시했는지, 게시 후 포털(교사·학생 로그인, 성적, 사진)이 정상인지, DB `graduates` 를 지웠는지.

이 세션(9/18~19)에 한 일 — 자세한 이유는 아래 각 절:
- 시트 메뉴 정리·이름 변경(🔥Firebase 동기화 / 🟰Record앱과 명단대조 / ⚙️관리자 도구 5개), 🔅신학년 준비(2·3월만, 3학년 자동 졸업),
  🚷 선택행 제외 처리(전학·자퇴·전근), DEFAULT_PW 1234, 이관 절차 `HANDOVER.md`
- 앱 이름 "광남고 포털", 카테고리 이름·설명(빛나다/동아리/교과/심화탐구), 홈 = 교사 학생찾기 검색창 + 초록 출석 띠 + 작은 카드 6개
- 프로그램 화면: 탭 없애고 제출자료 기본 + `⚙️ 프로그램 관리`. 출석 화면도 같은 구조(출석체크 기본 + 📊 통계 · ⚙️ 반 관리, 폴더 탭),
  야자는 교시 카드 + 학년 칩, **출석체크는 당일만**
- 학생별 모아보기·성적확인 검색 결과 = record 사진 카드 "이름 (학번)", 최종 규칙에 photo 읽기 추가(에뮬레이터 테스트 통과)
- 넓은 화면 상단바를 본문 폭에 맞춤, 우클릭·텍스트 선택 막기(입력칸 제외), 포커스 검은 테두리 제거
- ENGLISH = 그냥 링크(통합 로그인·학교 Google 계정 열 제거)

작업 환경 메모:
- 화면 확인은 목업 서버: 루트 `.claude/launch.json` 의 `portal-mock` (= `npm --prefix portal-main run mock`, :5178), 계정 t1/10101 비번 1234.
  목업 DB 는 메모리라 새로고침하면 초기화된다. 미리보기 캡처가 자주 잘리거나 확대돼 찍히니 수치(JS)로 검증했다.
- 규칙 테스트: Java 21·firebase-tools 를 이 PC 에 설치함(아래 "보안 규칙" 절 참고).
- 한글·이모지가 든 일괄 치환은 Edit 도구나 **파일로 저장한 파이썬 스크립트**로 한다(Git Bash heredoc 은 인코딩이 깨짐).

## 지금 상태

- **localhost 에서 실제 Firebase(record-99cf0)로 테스트 중. GitHub Pages 배포 전.**
- 이 폴더는 **git 저장소가 아니다.** 사용자가 GitHub 에 올릴 때 `firebase/test/node_modules`,
  `firebase/test/database-debug.log` 는 빼야 한다(파일이 9천 개 넘음).
- 사용자가 직접 해 둔 것: 이메일/비밀번호 로그인 켜기, Apps Script 배포(`js/config.js` 의 `SCRIPT_URL`),
  계정 시트, 전환기 보안 규칙 게시. (서비스 계정 키 `SA_VOCA`·`SA_NOVEL` 도 넣었었지만 ENGLISH 가 링크로 바뀌어 이제 안 씀)
- **최근 변경이 사용자 쪽에 반영됐는지 확인할 것** — 코드가 바뀔 때마다 사용자가 해야 하는 일:
  1. `gas/Code.gs` 를 Apps Script 에 붙여넣고 저장 (`doPost` 가 바뀌었으면 **새 버전으로 재배포**까지)
  2. `firebase/database.rules.json`(최종 규칙, 2026-09-19 부터) 을 Realtime Database 규칙에 붙여넣고 게시
  3. 브라우저 Ctrl+F5
  - 2026-09-19: 사용자가 메뉴 이름 변경까지의 Code.gs 를 붙여넣고 확인함. 그 뒤 ENGLISH 링크화로 Code.gs 가 또 바뀌었다(위 ▶ 절).
    시트 메뉴 폭(오른쪽 여백)은 구글 시트가 정하는 것이라 못 줄인다고 설명했고, 사이드바 대안은 사용자가 원하지 않았다.
    보안 규칙은 이 세션에 게시할 변경이 없다(photo 추가는 최종 규칙에만, 전환기 규칙은 그대로).

## 시트 메뉴 간소화 (2026-09-18)

사용자가 "메뉴가 너무 많고 헷갈린다" 고 해서 11개 → **최상위 2개 + 폴더 2개**로 줄였다.

```
🔥Firebase 동기화        🟰Record앱과 명단대조
⚙️관리자 도구 (🔑 선택행 PW 초기화 · 🚷 선택행 제외 처리 | ✅교사별 자료보기 · ❌자료 삭제 | 🔅신학년 준비)
```
(2026-09-19 사용자가 메뉴 이름을 직접 정해 위처럼 바꿨다. 함수 이름은 그대로 — syncAccounts, compareWithRecord,
resetSelectedPasswords, moveOutSelected, listPortalOwners, deletePortalDataByOwner, prepareNewYear)

- `① 계정 시트 준비` 메뉴는 없앴다. 하던 일(열 제목·**텍스트 서식**·구분 드롭다운·틀 고정)은 `prepareSheet_(sh)` 로 옮겨
  `syncAccounts` 가 실행될 때마다 자동으로 불린다. **ID·초기 비밀번호 열의 `@` 텍스트 서식은 꼭 필요하다** — 숫자로 바뀌면 `01234` → `1234`.
- **`🎓 선택한 행 졸업시트로 이관` 메뉴는 없앴다.** 어차피 새 학년도 준비 직전에 3학년을 골라 누르는 일이었으므로,
  `🔅신학년 준비` 가 **학번이 `3` 으로 시작하는 학생(`SENIOR_SID`)을 자동으로** 졸업 이관하도록 합쳤다(사용자 요청).
  엔진 `exportRows_` 는 그대로 남아 전학/자퇴와 자동 졸업 둘 다 쓴다.
- `🔅신학년 준비` 의 안전장치 (사용자가 하나하나 지정한 문구):
  - **2·3월이 아니면 실행 거부** ("새학년도 준비 기간이 아닙니다").
  - 확인창: "○○○○학년도 활동이 모두 끝났나요? 계속 진행하면 더이상 ○○○○학년도 활동을 관리하지 못합니다" + ①②③ 요약.
  - 완료창에 **다음 할 일 3단계**(학번 변경 → 신입생 추가 → 🔥Firebase 동기화)를 번호로 명시.
  - 같은 학년도에 두 번 누르면 다시 묻는다(`NEW_YEAR_DONE` 속성). 두 번 실행하면 새 학번이 지난 연도 열에 들어간다.
  - **`idColumnYear_()` 를 따로 뒀다** — `currentSchoolYear_()` 는 3월을 새 학년도로 보지만, 이 메뉴는 학번을 바꾸기 **전에**
    누르므로 3월에도 ID 열은 지난 학년도 학번이다. `exportRows_(…, {sidYear})` 로 연도를 명시해 넘긴다(안 넘기면 3월 실행 때
    졸업생 학번이 한 해 뒤 열에 적힌다).
  - 연도 열 생성(`yearCol_`)은 확인을 받은 **뒤에** 한다(취소 시 빈 열이 남지 않게).
  - 3학년 행에 UID 가 없으면(계정 미생성) 이관하지 않고 완료창에 개수만 알린다.
- 지움: `record 비밀번호로 채우기`(record 비밀번호를 더는 쓰지 않기로 함), `⛔ 사용 중지`·`✅ 사용 재개`(중지할 사유가 없다고 함).
- **사용 중지를 없애면서 생긴 구멍을 `🚷 선택행 제외 처리`로 메웠다.** 사용자는 "전학 가면 행 삭제" 로 알고 있었는데,
  시트 행만 지우면 Firebase 계정이 남아 **전학생이 계속 로그인된다.** 이 점을 알려 주고 메뉴를 새로 만들었다.
  자동 졸업과 같은 엔진(`exportRows_`)을 쓰고 시트 이름만 `전출`, 교사(전근·퇴직)도 같은 메뉴로 처리한다.
  계정을 지우지 않는 이유: `users/{uid}` 를 지우면 교사 통계·모아보기에서 그 학생 이름이 사라진다.
  - **학기 중 전출은 학번 재사용 금지**를 확인창·README 에 적었다. 전출 학생의 학번이 `sids` 에 `{학번: 올해}` 로 남으므로,
    같은 해에 전입생에게 같은 학번을 주면 `ownInYear` 판정이 겹쳐 **전입생이 전출 학생 자료를 읽는다.** 학번을 안 남기면
    교사 화면에서 전출 학생 자료에 전입생 이름이 찍히므로 더 나쁘다. **다음 학년도부터는 연도 꼬리표가 달라 안전하게 재사용된다**
    (사용자가 이 점을 물어봐서 `tests/years.test.mjs` 의 "전출 학생의 학번을 이듬해 후배가 받아도 겹치지 않는다" 로 못박아 두었다).
- `DEFAULT_PW = "1234"` 추가. 초기 비밀번호 칸이 비면 이 값으로 계정을 만든다(`pwOf_`). 첫 로그인 때 변경 강제는 그대로.
  전원 초기화는 시트 2행~끝까지 선택 → `🔑 초기화` → 공통 비밀번호 입력.
- `🟰Record앱과 명단대조`(`compareWithRecord`, 옛 `record 학생 명단 가져오기`)는 **읽기 전용**이다.
  올해 학번(ID 열) 기준으로 한쪽에만 있는 학생을 HTML 모달로 보여 주기만 하고, 행 추가·상태 쓰기를 하지 않는다.
  사용자가 "보고 두 시트를 직접 맞추겠다" 고 했다. 자동 추가/전출은 일부러 안 한다 — record 업로드가 누락되면 멀쩡한 학생이 잘려 나간다.
- `🔧 제출자료 저장 구조 변환`(`migrateSubmissionsByProgram`)은 **완전히 삭제했다.** 개발 초기라 실제 제출자료가 하나도 없어서
  예전 구조(`portal/submissions/{학번}/{자료}`)로 저장된 자료가 있을 수 없다. 다시 만들 필요 없다.

## 동기화 = 시트가 정답 (2026-09-19)
- 사용자가 "행을 지우고 동기화해도 Firebase 에 살아 있어 로그인된다 — 진짜 동기화가 아니다" 라고 지적. 선택지 3개(잠금/잠금+완전삭제 메뉴/완전삭제) 중 **잠금**을 골랐다.
- `syncAccounts` 0단계: DB `users` 중 role 이 있고 `active !== false` 인데 계정 시트에 UID 가 없는 사람 → 목록 확인창 → [예] 면 `blockMissing_`
  (🚷 제외 처리와 같은 `blockAccounts_` + `전출` 시트에 DB 정보로 한 줄 기록, 상태 "(시트에서 삭제됨)"). [아니요] 면 막지 않고 완료창에 경고.
  **완전 삭제는 하지 않는다** — 기록이 학번으로 저장돼 있어 users 를 지우면 이름이 사라지고, 학번 재사용 시 남의 이름이 찍힌다.
- 계정 시트가 비어 있으면 동기화 자체를 멈춘다(전원 잠금 방지). ID 가 빈 행도 UID 가 있으면 "시트에 있음"으로 본다.
- 되살리기: 잠긴(`active:false`) 계정의 행이 UID 와 함께 계정 시트에 있으면 `disableUser:false`, active true, leftAt·graduated null,
  로그인 ID 는 기존 ID 변경 흐름이 out-… → 학번으로 되돌린다. 상태 "✅ 다시 사용 (잠금 해제)".
- 잠그기 전에는 학번 재사용 시 동기화의 EMAIL_EXISTS 복구 경로 때문에 **전입생이 떠난 학생 계정을 물려받는** 문제가 있었다. 잠그면 이메일이 out-… 로 옮겨져 사라진다.
- 가짜 시트·가짜 Firebase 로 잠금 → 학번 비움 → 되살리기 → [아니요] → 빈 시트 멈춤 5가지를 돌려 확인(스크래치 스크립트, 저장소엔 없음). 실제 시트에서는 아직.
- `🚷 선택행 제외 처리` 는 그대로 둔다(즉시 처리·골라서 처리하는 지름길). 사용자에게 제외 처리를 권장한다고 설명.

## 남은 일

- [ ] `🚷 선택행 제외 처리` 와 `🔅신학년 준비`(자동 졸업 포함)는 실제 시트에서 아직 안 돌려 봤다. 테스트 학생 2~3명으로 먼저 해 볼 것
- [ ] 배포 전 `js/config.js` 의 `ENGLISH_APPS` 주소가 실제 배포 주소인지 확인 (테스트 중 localhost 로 바꿨을 수 있음)
- [ ] 포털을 GitHub Pages 에 올리기 (예: `puroome.github.io/portal/`)
- [ ] 사용자가 할 일: Apps Script 스크립트 속성 `SA_VOCA`·`SA_NOVEL` 삭제 + 두 Firebase 프로젝트의 그 서비스 계정 키 삭제
      (다른 프로젝트 관리자 권한 키라 안 쓰면 없애는 게 안전). `계정` 시트의 `학교 Google 계정` 열도 지워도 된다(코드는 무시함).
- [ ] 예전 PC 에 남아 있다던 voca·novel 의 **SSO 수정본은 이제 올릴 필요 없다**(ENGLISH 가 바로가기로 바뀜).
      다만 그 PC 의 novel 미커밋 변경 중 SSO 와 무관한 것이 있었는지는 모른다 — 그 PC 에서 확인할 것.
- [ ] `🔥Firebase 동기화` 의 **ID 두 단계 교체**(임시 ID 경유)도 실제 시트·Firebase 에서 한 번도 돌려 보지 못했다.
      시트 읽기 부분만 가짜 시트로 검증(`tests/codegs-sheet.test.mjs`).
- [ ] (제안만 함, 미결정) 담당 교사를 다른 교사에게 넘기는 기능, 담임이 앱에서 자기 반 비밀번호 초기화
- [ ] **record 앱은 2026-09-19 부터 학생이 직접 쓰지 않는다**(사용자 확인). 남은 일: 최종 규칙(`database.rules.json`)으로 교체.
      record 의 Apps Script(`record-main/Coge.gs`)는 성적 업로드용으로 계속 쓴다 — 비밀번호를 올리지 않게 고쳤고(`students` 를 PUT 하므로
      다음 동기화 때 `pw` 가 DB 에서 사라짐), `registerPassword`·`doGet`(웹 앱)·admin 시트용 `teacher`/`requestPermission` 을 없앴고(포털은 `admins` 를 안 씀 — 동기화 때 DB `admins` 삭제), 학년 시트 C열 "비밀번호"가 있든 지워졌든 성적을 같은 열에서 읽는다
      (머리글 1~5행 C열이 "비밀번호"면 건너뜀). Coge.gs 에 DB 비밀키(FIREBASE_SECRET)가 있으니 GitHub 에 올리지 말 것.

## 사용자와 일하는 방식 (지켜야 할 것)

- **사용자는 Firebase·Drive 에 대한 쓰기 권한을 주지 않는다.** DB 를 바꿔야 하면 사용자가 실행할
  **시트 메뉴(Apps Script)** 를 만들어 주고, 지우기 전에 목록·개수를 보여 주고 확인받게 한다.
  (예: `❌자료 삭제`, `🚷 선택행 제외 처리`)
- 사용자는 교사이고, 단계별 **번호 매긴 할 일**과 클릭 위치를 좋아한다. 안내는 짧게, 표 활용.
- voca·novel 은 **독립 앱으로 두고 포털은 입구(링크)만** 한다. 사용자가 "연결만 해 놓은 것"을 좋아했다.
  두 앱을 포털로 흡수하는 방향은 사용자가 원할 때만.

## 반드시 알아야 할 설계 결정

### 보안 규칙
- 규칙은 **`firebase/test/build-rules.mjs` 에서 생성**한다. json 두 개(최종·전환기)를 직접 고치지 말고
  이 파일을 고친 뒤 `npm run build:rules` → `npm test` → `npm run test:transition` (폴더: `firebase/test`).
  Java 21 이 필요(`C:\Program Files\Eclipse Adoptium\jdk-21...`). 예전 PC 는 PATH 에 Java 8 이 먼저 잡혀서
  `JAVA_HOME`·`PATH` 를 명령 앞에서 지정해 돌렸다. 지금 PC(2026-09-19 설치)는 `jdk-21.0.12.101-hotspot` 과
  `%APPDATA%\npm\firebase.cmd`(firebase-tools 15.30). 설치 전에 열린 셸은 PATH 를 모르니 `JAVA_HOME`·`PATH` 를 앞에 붙여 실행.
  테스트 결과는 22개 중 20 통과·2 건너뜀이 정상(최종/전환기 전용 테스트가 서로 건너뜀).
- **2026-09-19 최종 규칙(`database.rules.json`)으로 교체**(record 앱을 학생이 더 쓰지 않음). 앞으로 게시할 파일은 이것이다.
  전환기 규칙은 되돌릴 때만. record `Coge.gs` 는 DB 비밀키로 쓰므로 규칙과 무관하게 성적 업로드가 된다.
  최종 규칙 테스트에 "graduates·admins·students/pw·students 전체는 비로그인·교사·학생 모두 못 읽음"을 추가했다(최종·전환기 둘 다 20 통과·2 건너뜀).
  DB 에 남은 `graduates`(졸업생 전화번호)는 가져오기가 끝나 필요 없으니 콘솔에서 지우라고 안내했다.
- 과거에 찾은 규칙 버그 두 가지 — 다시 만들지 말 것:
  - `.validate` 에 `createdByUid` 를 **필수로** 넣으면 예전 반(값 없음)을 아무도 못 고친다.
  - `sids/$sid.val() === 프로그램.year` 처럼 비교하면 **둘 다 null 일 때 참**이 되어 남의 자료가 열린다 → `.exists()` 로 먼저 확인.

### 학번은 해마다 다른 학생에게 다시 쓰인다 (가장 중요)
- 사람은 **UID** 로, 학번은 학년도별 꼬리표로 본다: `users/{uid}/sids = { 학번: 학년도 }` (계정 시트 ID 열 + `2025년` 같은 연도 열).
- 규칙 `ownInYear`: "그 해에 그 학번이었던 학생"만 그 해 자료를 읽는다. 쓰기는 **지금 학번 + 지금 학년도**만.
- 교사 화면의 이름 찾기는 `getStudentMap(year)` (그 해 기준). **여러 학년도 자료를 한 표에 섞지 않는다** —
  야자 통계는 기간이 두 학년도에 걸치면 학년도별로 표를 나눈다(섞었더니 신입생 이름이 선배 기록에 찍혔다).
- 계산은 `js/years.js`(테스트 `tests/years.test.mjs`).
- 학년도 전환 순서(매년 2월 말~3월 첫 수업 전): 🔅신학년 준비(3학년 자동 졸업 포함, **학번 바꾸기 전에**) → 학번·학년·반·번호 수정·신입생 추가 → 🔥Firebase 동기화.
  12월에 하면 그해 남은 활동(겨울 방과후·야자 체크인·제출)이 막힌다.
- 계정 시트는 **헤더 이름으로 열을 찾는다** (`loadCols_`). 연도 열 위치는 자유.

### 출석 (방과후·야간자율)
- 교시·시간 고정(`js/config.js` 의 `ATT_PERIODS`, `ATT_DAY_PERIODS`). 지각 = 첫 교시 시작 15분 뒤.
- 상태는 출석·지각·조퇴·결석 4개, **미체크 = 결석**.
- 방과후는 **등록 교사만**, 야자는 **모든 교사**. 야자는 반 이름 없이 학년+교시, 명단은 그 학년 `NIGHT_CLASS`(1반) 자동.
- 야자는 그날 **감독교사 확인**(`portal/att/sessions/{gid}/{date} = {uid,name}`) 후에만 체크 가능, 확정되면 다른 교사 잠김.
- **통계는 감독 확인된 날만 센다**(`sessionDates`). 감독을 내려놓으면 그날은 통계에서 빠진다. 계산은 `js/att-schedule.js`.
- 출석 화면엔 종료된 반을 안 띄운다. `종료된 수업 포함`은 반 관리·통계에만.
- **출석체크는 당일 운영하는 반만** (사용자 요청). 방과후의 `운영중 수업전체` 체크박스는 없앴다(다른 요일 반을 체크할 수 있었음).
  목록만 막으면 학생별 모아보기의 출결 카드·주소로 들어올 수 있어서 `renderGroup` 에서도 `runsOn`·종료·학년도를 확인해
  안내 + 통계 바로가기만 보여 준다. 보안 규칙은 요일을 모르므로(교사 쓰기 허용) 막는 곳은 화면뿐이다.
- 교사 화면 구조(2026-09-19, 사용자 요청으로 프로그램 화면과 통일): `#/att` 출석체크가 기본, 머리 오른쪽 `📊 통계`(#/att/stats)·
  `⚙️ 반 관리`(#/att/manage). 반 관리 머리에 `+ 새 반`. 방과후/야간자율은 폴더 탭(`.folder-tabs`/`.folder-panel`).
  통계 위치는 사용자가 물어서 "출석체크 화면 버튼" 으로 제안·적용했다(반 관리 안에 넣으면 통계가 관리 업무처럼 숨어 버림).
- 프로그램(교사) 화면도 같은 구조: 제출자료가 기본, `⚙️ 프로그램 관리`(#/p/{cat}/manage). 예전 두 탭은 없앴다.

### 학생 사진 카드 (학생별 모아보기·성적확인 검색 결과)
- record 앱이 쓰던 `students/{학번}/photo`(사진 주소)를 읽어 3:4 사진 + "이름 (학번)" 카드로 보인다. `directory.js` 의
  `studentCard`·`hydratePhotos`. 사진이 없거나 주소가 깨지면 👤.
- **최종 규칙에 `students/$sid/photo` 읽기(교사·본인)를 추가했다.** 전환기 규칙은 students 전체 공개라 지금은 규칙 게시가 필요 없다.
  `rules.test.mjs` 의 "성적" 테스트에 사진 검사(교사·본인 읽기 O, 남의 사진 X, 쓰기 X)를 넣었고 최종·전환기 모두 통과(2026-09-19).
- lazy 이미지는 **숨겨 두면(display:none) 브라우저가 안 불러온다.** 그래서 투명(opacity 0)하게 겹쳐 붙였다가 onload 때 보인다.
  미리보기 탭이 백그라운드(visibilityState hidden)면 lazy 이미지가 안 불러와지니 확인할 땐 `loading = "eager"` 로 바꿔 볼 것.

### 드라이브 저장 계정 (2026-09-18 논의)
- `appsscript.json` 의 `executeAs: USER_DEPLOYING` 때문에 **제출 파일은 "웹 앱을 배포한 계정" 드라이브에 저장된다.**
  지금은 사용자 개인 계정(`puroome@gmail.com`). 학교 공용 계정(`ceb1004@ai.jne.kr`)으로 옮기는 방법을 안내했으나
  **당분간 개인 계정 유지**로 결론. 옮기려면: 시트를 그 계정에 편집자 공유 → GCP IAM 에 편집자 추가(doPost 가 DB 를 읽음) →
  스크립트 속성 `ROOT_FOLDER_ID` 삭제 → 그 계정으로 새 배포 → `SCRIPT_URL` 교체.
- `doGet` 이 `deployedAs`(이메일 앞 3글자만)·`driveFolder` 를 돌려주므로 주소창으로 배포 계정을 확인할 수 있다.
- **카테고리·교사별로 저장 계정을 나눌 수는 없다** — 폴더는 지정 가능하지만 용량은 항상 파일을 만든 계정(배포 계정)에서 차감된다.
  `setOwner()` 는 개인 Gmail 간에 신뢰할 수 없다. 사용자에게 이 점을 설명했고, 목적을 묻는 질문은 사용자가 닫았다.
- **`🔒 담당자만` 은 앱 안에서만 지켜진다.** 드라이브·Firebase 소유자는 전부 볼 수 있다(README 에도 적었다).
- 이관 절차는 **`HANDOVER.md`** 에 따로 정리했다(사용자가 미리 준비해 달라고 요청).
  핵심은 **파일을 복사하면 안 된다**는 것 — DB `files[].id` 가 드라이브 파일 ID 이고, 권한 정보는 파일 설명란에 있어
  복사본에는 따라오지 않는다. 반드시 소유권 이전. 개인 Gmail → Workspace 도메인 이전이 막히는 경우의 대안 3가지도 적어 뒀다.

### 전화번호 (2026-09-19)
- `계정` 시트 `전화번호` 열(기본 J) → 동기화 때 `users/{uid}/phone`(빈 칸이면 null 로 지움). users 는 교사·본인만 읽으므로 규칙 변경 없음.
- 학생 정보 화면(`students.js` renderProfile) 머리에 `📞 전화`(tel:)·`📩 문자`(sms:) — roll 앱과 같은 방식·색. 번호 없으면 버튼 안 보임.
- `phoneOf_`: 숫자만 남기고, 시트가 숫자로 바꿔 0 이 빠진 `1012345678` 은 `0` 을 붙여 되살린다.

### 졸업생 (2026-09-19)
- record 앱의 `old` 시트(→ DB `graduates`)를 없애고 포털 `🎓 졸업생`(`js/alumni.js`, 교사 전용 홈 카드, `#/alumni/{연도}`)으로 옮겼다.
- 옮긴 방법(2026-09-19 사용자가 실행 완료 → 메뉴·함수 삭제함, 다시 만들 필요 없음): DB `graduates` 를 읽어 `🔅신학년 준비` 졸업 처리와 같은 모양
  (졸업연도 시트에 행, `users/rec{연도}_{학번}` = {graduated, sids:{3학년학번: 연도-1}, phone, photo, active:false}). **Auth 계정은 안 만든다**(UID 칸의 `rec…` 는 가짜 키).
  old 시트엔 2025·2026 두 해 졸업생이 있어 둘 다 옮긴다. record `Coge.gs` 는 더 이상 `graduates` 를 올리지 않는다(DB 에 남은 건 가져오기용 원본).
- 연락처 창은 사용자가 준 캡처대로 단순하게: 제목 = 이름만, 사진, "졸업 · 반 번호", 전화/문자(없으면 "연락처가 없습니다."), **버튼 줄 없음**.
  바깥 클릭으로 닫힘 — `ui.modal` 에 `dismissible`·`className` 옵션을 추가(버튼이 하나도 없으면 버튼 줄을 안 그림).
- `ui.modal` 의 `closeX` 옵션: 오른쪽 위 모서리 ✕(`.modal-x`, 바깥 모서리 곡률 16px = 창과 같음, 연한 빨강). 사용자 요청으로
  사용자 메뉴(이름 칩 → `🔑 PW 변경`·로그아웃, `닫기` 줄 없앰, **폭은 내용에 맞춤(width:auto)** — 사용자가 오른쪽 여백을 싫어함)와 졸업생 연락처 창에 씀. 바깥 클릭도 그대로 닫힘.
- **졸업 후 정보 메모**(2026-09-19): 졸업생 창 왼쪽 위 `ℹ️`(`.modal-i`, ✕ 와 좌우 대칭·노란 바탕, 메모 개수 배지) → 창 본문이 Y축으로 뒤집힘
  (`.flip`/`.flip-inner`/`.flip-face`, 제목=이름은 그대로). 뒷면 = 메모 목록(최신순, "2026.09.19 기록 · … 수정") + 입력칸 + `메모 남기기`.
  `portal/alumniNotes/{졸업생 uid}/{nid}` = {text, createdBy, createdByUid, createdAt, updatedAt}. **작성자 이름은 저장하되 화면엔 안 보임**(사용자 지시).
  규칙: 교사만 읽기, 쓰기는 자기 uid·이후 쓴 교사만, text 1~1000자(테스트 추가, 24개 중 22 통과·2 건너뜀). 관리자 도구 두 메뉴에 메모 개수·삭제 포함.
  (수정) ℹ️ 이모지는 뺐다 — 메모가 없으면 노란 모서리만, 있으면 그 자리에 빨간 동그라미 숫자. 창 높이는 **앞면 기준 고정**:
  뒷면은 `position:absolute; inset:0` 으로 앞면 위에 겹치고 메모 목록만 스크롤. 창은 `overflow:hidden` —
  3D 회전의 가상 넘침 때문에 내용이 짧아도 창에 스크롤바가 생겼던 문제(사용자 캡처)를 이걸로 없앴다.
  (추가) **학생 정보 화면 `ℹ️ 메모`**(전화 왼쪽, 노란 버튼·개수 배지, `📊 성적 보기`→`📊 성적`): 졸업생 뒷면과 같은 창(250×320, 노란 모서리 개수, ✕).
  메모 코드는 `js/person-notes.js` 로 뺐고 **같은 경로(`portal/alumniNotes/{uid}`)를 공유** — 사람(uid)에 붙어 재학 중 메모가 졸업 후 카드로 이어진다.
  경로 이름은 규칙이 이미 게시돼 그대로 둠(규칙 변경 없음).
  (분리) 메모에 `kind` = "school"(학생 정보 화면에서 씀) | "after"(졸업 카드에서 씀). 꼬리표 없는 예전 메모는 `noteKind`: 졸업연도 3/1 이전 = school.
  학생 정보 창 = 재학 중만(제목 "재학 중 메모"). 졸업 카드 뒷면 = 탭 `졸업 후 N | 재학 중 M`(기본 졸업 후), 재학 중 탭은 회색·입력칸 없음(내 메모 수정·삭제는 됨).
  **졸업 카드 모서리 숫자 = 졸업 후만.** 사용자가 목업 4장면(show_widget)을 보고 "A안(쓴 곳으로 구분)+탭" 을 골랐다. kind 는 규칙에 없어도 저장됨(추가 필드 허용).
  미리보기 창은 CSS 전환을 멈춰 두어 뒤집힘이 안 보일 수 있다 — `transition:none` 으로 바꿔 재면 matrix3d(-1…) 로 정상.
- 앨범은 `users` 중 `graduated` 가 있는 학생 전부(앞으로 포털이 졸업 처리한 학생 포함). 사진 = `users.photo` 또는 `GRAD_PHOTO_BASE{연도}_{3학년학번}.jpg`.

### 🪧 공지 (2026-09-19)
- `portal/notices/{nid}` = {title, content, startDate, endDate("YYYY-MM-DD"), createdBy, createdByUid, createdAt, updatedAt}. `js/notices.js`.
- 규칙: 읽기 = 로그인한 누구나(학생도 창을 봐야 함), 쓰기 = 교사 + 새로 만들 땐 자기 uid, 이후 만든 교사만. validate: 필수 4개·문자열·시작≤종료·createdByUid 불변. 테스트 "공지" 추가(최종·전환기 23개 중 21 통과·2 건너뜀).
- 교사 메뉴 `#/notice`(진행 중·예정·지난 공지, 카드 누르면 상세) + `⚙️ 공지 관리` `#/notice/manage`(`+ 새 공지`, 내 공지만 수정·삭제 버튼, 남의 것은 🔒 문구). 학생은 메뉴 없음(주소로 와도 홈).
- 앱 열 때 창: `app.js` 로그인 후 `route()` 다음 `showActiveNotices()` — 페이지 로드당 한 번, 기간 중 공지를 종료일 빠른 순으로 한 창에, 제목·D-n(당일 "오늘까지")·"○/○까지 · 담당", `자세히보기 ▾` 펼침. 닫기 버튼 + 바깥 클릭.
  (수정) 창 모양은 졸업생 창과 통일: `닫기` 줄 없이 모서리 ✕(`closeX`), 크기 고정 폭 250 · 높이 320px(`.notice-pop-modal`),
  공지 목록(`.nt-pop-list`)만 스크롤, 체크박스는 아래 고정. 교사 메뉴의 공지 상세 창(`.notice-modal`)은 그대로.
  **교사만** `오늘은 다시 뜨지 않기` 체크박스(사용자 요청). localStorage `noticeHide:{uid}` = {date, ids} — 그날 본 공지만 숨기고,
  그날 새 공지가 생기면 다시 뜬다. 학생은 체크박스 없이 항상 뜬다.
- 관리자 `✅교사별 자료보기`·`❌자료 삭제` 에 공지 개수·삭제 포함.
- 홈 순서(사용자 지정): 공지(교사)·급식·행사·성적·빛나다·동아리·심화탐구·교과·ENGLISH·졸업생(교사). `성적확인` → `성적`(카드·제목). CATEGORIES 순서를 bitnada·club·contest·subject 로 바꿈(키는 그대로).

### 🍴 급식 · 📅 행사 — NEIS (2026-09-19)
- 명세서 `D:/Downloads/NEIS_INTEGRATION.md` 기반. 인증키는 사용자가 줌 → **Apps Script 중계**(사용자 선택). 키는 스크립트 속성 `NEIS_API_KEY` 에만, 저장소 어디에도 적지 않았다.
  (NEIS 는 CORS `*` 를 주므로 브라우저 직접 호출도 가능하지만 키가 노출되어 중계를 택함)
- 서버 `neis_`(doPost action `neis`, `verifyUser_` 로 로그인 필요): 필요한 필드만 줄여 `{rows}` 반환, INFO-200 → 빈 배열, ERROR-500/600/601 한 번 재시도,
  CacheService 6시간(한글이 3바이트라 한 달 급식이 100KB 를 넘을 수 있어 put 실패는 무시).
- **명세서와 실제 응답이 다른 점**(실제 호출로 확인): 알레르기 번호가 `육개장 (1.9.16)` 괄호 형식, 학년 필드가 `TW_GRADE_EVENT_YN`(TWO_ 아님),
  `SBTR_DD_SC_NM` 은 수업일에 "해당없음". 토요휴업일이 1년에 49건·방학이 날마다 들어와서 토요휴업일은 숨기고 같은 이름 3일 이내 간격은 기간으로 묶는다.
- 앱: `js/neis-parse.js`(순수 계산, `tests/neis.test.mjs`), `js/neis.js`(중계 호출 + localStorage `neis:meal:{YYYYMM}` 6h · `neis:schedule:{학년도}` 7d, 실패 시 지난 캐시),
  `js/school-info.js`(화면). 홈 카드 맨 앞 두 개(🍴 급식 · 📅 행사), 교사·학생 모두.
- 급식: 주말은 건너뛰며 이동(버튼·스와이프·←→), 기본 중식, 조식·석식 있으면 탭, 고른 식사는 날짜를 옮겨도 유지. 알레르기 번호 배지를 누르면 이름 토스트.
- 행사: 상단 "오늘 / D-n" 배너, 학년 칩(기본 전체), 월 달력(휴업일 빨강 칸, 일정 점), 날짜 탭 → 아래 상세, 그 달 일정 목록.
- 목업: `dev/neis-sample.json`(2026-08~10 급식·2026학년도 일정, 키 없음)을 `dev/mock-firebase.js` 가 fetch 가로채기로 돌려줌. `?neisFail` 로 오류 흉내.

### 활동 프로그램
- 관리는 만든 교사만. `private`(교과·동아리 기본)면 열람도 만든 교사만. 제출자료 경로 `portal/submissions/{pid}/{sid}/{id}`.
- 빛나다·동아리는 제출 기간 없음, 심화탐구·교과만 시작/마감일. 동아리 명단은 반을 골라도 **꺼진 상태**로 시작.

### ENGLISH
- **그냥 링크다**(2026-09-19, 사용자 결정). 새 탭에서 `ENGLISH_APPS` 주소를 바로 연다.
- 예전 "통합 로그인"(Apps Script 가 서비스 계정 키로 커스텀 토큰을 만들어 `앱주소#sso=토큰`)은 **없앴다.** 이유:
  ① 누를 때마다 Apps Script 왕복 1.5~4초(측정) ② 배포된 voca·novel 에 토큰 받는 코드가 없어 효과도 없었음
  ③ 학생들은 voca·novel 을 쓰던 **자기 기기**로 포털에 오므로 그 브라우저 로그인으로 바로 들어감 — 사용자가 짚은 점.
  처음 쓰는 학생은 직접 접속과 똑같이 Google 로그인 → 각 앱 승인 절차.
- 그래서 Code.gs 의 `englishToken_`·서비스 계정 함수들, `계정` 시트의 `학교 Google 계정` 열(HEADERS 에서 뺌),
  `users/{uid}/googleEmail`(동기화 때 null 로 지움)을 모두 없앴다. 예전 시트에 J열이 남아 있어도 헤더 목록에 없으니 무시된다.

## 테스트 방법

| 무엇 | 명령 (폴더) |
|---|---|
| 앱 계산 규칙(출결 시간·연도별 학번·계정 시트 읽기) | `npm test` (portal-main) |
| 보안 규칙 | `npm test`, `npm run test:transition` (firebase/test, Java 21) |
| 화면 확인 (실제 DB 안 건드림) | `npm run mock` → http://localhost:5178 (portal-main) |

- 가짜 서버 계정(비밀번호 1234): 교사 `t1`(김교사)·`t2`(박교사), 학생 `10101`·`10102`·`20101`(작년 10103 선배)·`10103`(올해 신입생).
- 실제 Firebase 로 볼 때는 정적 서버로 이 폴더를 띄운다 (예: `python -m http.server 8780`).
- 사용자 PC 의 Git Bash 에서 한글이 든 heredoc 을 python 에 넘기면 인코딩이 깨져 치환이 실패한다.
  한글이 들어간 수정 스크립트는 **파일로 저장한 뒤 실행**했다.
